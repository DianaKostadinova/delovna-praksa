{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "//": "React Router owns /blog, /articles/3, /pharmacy and the rest. Without this, a hard refresh or a shared deep link would 404 against Vercel's static file server before the app ever loads.",
      "source": "/((?!assets/|images/|favicon|.*\.[a-zA-Z0-9]+$).*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
